# Database
POSTGRES_URL=postgresql://dev_user:dev_password@localhost:5432/postgres_dev

# Better Auth
BETTER_AUTH_SECRET=change-me
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# App URL (usado no resolver de tenant por host/subdomínio)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI (opcional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Storage (opcional, se usar blob)
BLOB_READ_WRITE_TOKEN=

# Redis (opcional, apenas se for usar rate limiting)
REDIS_URL=
